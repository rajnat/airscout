def fetch_historical_price(origin, destination, departure_date):
    # Mock function: Replace with real implementation
    historical_prices = {
        "LON": 3000,
        "PAR": 2500,
        "TOK": 3500,
    }
    return historical_prices.get(destination, None)
